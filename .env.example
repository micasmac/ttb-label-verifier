# V1 runs entirely in demo mode — no API key is used or required.
#
# Future version — live AI extraction:
# To enable, follow the steps in app/services/extraction.py, then copy this
# file to .env and set:
# ANTHROPIC_API_KEY=
# EXTRACTION_MODEL=claude-sonnet-4-20250514
